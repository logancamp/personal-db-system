services:
    vault:
        container_name: "vault-myapp"
        image: hashicorp/vault:1.18
        environment:
            VAULT_DEV_ROOT_TOKEN_ID: "${VAULT_TOKEN:?run ./boot-dev.sh or set VAULT_TOKEN}"
        ports:
            - "127.0.0.1:8200:8200"
